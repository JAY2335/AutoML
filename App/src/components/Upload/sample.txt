
            
               
            <form className="options">
            

            
                
            <div>
            
            <input type="radio" id="mean" name="fill" value="mean" />
            <label htmlFor="mean"><h2>Mean</h2></label>
            </div>
            <div>
            <input type="radio" id="median" name="fill" value="median" />
            <label htmlFor="median">Median</label>
            </div>
            <div>
            <input type="radio" id="mode" name="fill" value="mode" />
            <label htmlFor="mode">Mode</label>
            </div>
            </form>
            <label htmlFor="cars">Choose a car:</label>
            <form>


            function preprocess(str){
    let lines=str.split('\n')
    let result=[];
    let columnname=lines[0].split(',')
    lines.slice(1,length(lines)).forEach(line=> {
        let linelist=line.split(",")
        let tempJson=new JSON();
        for(let i=0;i<length(linelist);i++){
            tempJson.columnname[i]=linelist[i];

        }
    result.push(tempJson)
        
    });
    

}
////////////


const headers = {
        Key: '-Lt_tWbTpuvtgkTHmhk9LA',
        "content_type": 'application/json'
    };

    const DUMMY=[{
        "first_name": "Constantin",
        "last_name": "Langsdon",
        "email": "clangsdon0@hc360.com",
        "gender": "Male",
        "age": "96"
       }]